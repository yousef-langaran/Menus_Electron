# خلاصه پیاده‌سازی اپلیکیشن Electron

## ✅ ویژگی‌های پیاده‌سازی شده

### 1. ثبت سفارش مشتری
- رابط کاربری مشابه نسخه وب
- انتخاب محصولات از منو
- مدیریت سبد خرید
- ثبت اطلاعات مشتری
- پشتیبانی از تخفیف دستی

### 2. کار آفلاین
- ذخیره سفارشات در IndexedDB (Dexie)
- امکان ثبت سفارش بدون اتصال به اینترنت
- نمایش وضعیت آنلاین/آفلاین

### 3. همگام‌سازی خودکار
- بررسی وضعیت اتصال هر 30 ثانیه
- ارسال خودکار سفارشات آفلاین به سرور
- امکان همگام‌سازی دستی از صفحه تنظیمات

### 4. کش منو و اطلاعات ورود
- ذخیره منو در حافظه محلی
- ذخیره اطلاعات کاربر و توکن
- استفاده از داده‌های کش شده در حالت آفلاین

### 5. چاپ رسید
- چاپ همزمان به دو پرینتر (یا بیشتر)
- چاپ بدون باز شدن پنجره
- فرمت رسید فارسی و خوانا
- انتخاب پرینترها از رابط کاربری

## 📁 ساختار پروژه

```
Menus_Electron/
├── electron/              # Main Process (Node.js)
│   ├── main.ts           # نقطه ورود Electron
│   ├── preload.ts        # Bridge بین Main و Renderer
│   ├── database/         # مدیریت دیتابیس
│   │   ├── orders.ts     # ذخیره سفارشات آفلاین
│   │   └── cache.ts      # کش منو و کاربر
│   ├── services/         # سرویس‌های اصلی
│   │   ├── sync.ts       # همگام‌سازی سفارشات
│   │   └── printer.ts    # چاپ رسید
│   ├── config/           # تنظیمات
│   │   └── api.ts        # تنظیمات API
│   └── utils/            # توابع کمکی
│       └── network.ts   # بررسی وضعیت شبکه
│
├── src/                  # Renderer Process (React)
│   ├── pages/           # صفحات برنامه
│   │   ├── Login.tsx    # صفحه ورود
│   │   ├── Order.tsx    # صفحه ثبت سفارش
│   │   └── Settings.tsx  # صفحه تنظیمات
│   ├── store/           # State Management (Zustand)
│   │   ├── authStore.ts # مدیریت احراز هویت
│   │   └── orderStore.ts # مدیریت سفارشات
│   └── services/        # سرویس‌های فرانت
│       ├── api.ts       # ارتباط با API
│       ├── cache.ts     # کش در فرانت
│       └── offlineStorage.ts # ذخیره آفلاین
│
└── package.json         # وابستگی‌ها و اسکریپت‌ها
```

## 🚀 نحوه استفاده

### نصب
```bash
cd Menus_Electron
npm install
```

### اجرا در حالت توسعه
```bash
npm run dev
```

### ساخت فایل اجرایی
```bash
npm run build
npm run dist
```

## 🔧 تکنولوژی‌های استفاده شده

- **Electron**: فریمورک اصلی
- **React**: رابط کاربری
- **TypeScript**: زبان برنامه‌نویسی
- **Vite**: Build tool
- **Zustand**: State management
- **Dexie**: IndexedDB wrapper
- **Axios**: HTTP client

## 📝 نکات مهم

### 1. اتصال به API
برنامه به صورت پیش‌فرض از `https://apimenu.promal.ir/api/v1` استفاده می‌کند.
برای تغییر، فایل `src/services/api.ts` را ویرایش کنید.

### 2. ذخیره‌سازی آفلاین
- سفارشات در IndexedDB ذخیره می‌شوند
- منو و اطلاعات کاربر در localStorage ذخیره می‌شوند
- داده‌ها در پوشه userData سیستم عامل ذخیره می‌شوند

### 3. چاپ رسید
- پرینترها باید قبل از استفاده به سیستم متصل باشند
- می‌توانید چندین پرینتر را انتخاب کنید
- چاپ به صورت متوالی انجام می‌شود (برای جلوگیری از تداخل)

### 4. همگام‌سازی
- همگام‌سازی خودکار هر 30 ثانیه انجام می‌شود
- می‌توانید به صورت دستی از صفحه تنظیمات همگام‌سازی کنید
- در صورت خطا، سفارش در حالت آفلاین باقی می‌ماند

## 🐛 عیب‌یابی

### مشکل در نصب
```bash
rm -rf node_modules package-lock.json
npm install
```

### مشکل در کامپایل
```bash
npm run build:electron
```

### مشکل در چاپ
- بررسی اتصال پرینتر
- بررسی لاگ‌های کنسول
- تست با یک پرینتر اول

## 📞 پشتیبانی

برای مشکلات یا سوالات، لطفاً issue ایجاد کنید.

